p({
  class: "foam.core.boot.CSpec",
  name: "safBroadcastReceiverTestDAO",
  authenticate: false,
  pm: false,
  service: {
    class: "foam.core.saf.test.SAFBroadcastReceiverTestDAO"
  }
})

p({
  "class": "foam.core.boot.CSpec",
  "name": "safBroadcastReceiverDAO",
  "serve": true,
  "serviceScript": `
    return new foam.dao.EasyDAO.Builder(x)
      .setOf(foam.core.saf.SAFEntry.getOwnClassInfo())
      .setNullify(true)
      .setCluster(false)
      .setSaf(false)
      .setRuler(false)
      .setDelegate(x.get("safBroadcastReceiverTestDAO"))
      .build();
   `
})

p({
  "class": "foam.core.boot.CSpec",
  "name": "safTestObjectDAO",
  "serve": true,
  "serviceScript": `
    return new foam.dao.EasyDAO.Builder(x)
      .setOf(foam.core.saf.test.SAFTestObject.getOwnClassInfo())
      .setFuid(true)
      .setCluster(false)
      .setSaf(true)
      .build();
   `
})
